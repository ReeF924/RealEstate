@{
    List<User> users = (List<User>)this.ViewBag.Users;
}
<div>
    <div>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Username</th>
                    <th>Type</th>
                    <th>First name</th>
                    <th>Last name</th>
                    <th>Email</th>
                    <th>Phone number</th>
                    <th>Change Type</th>
                    <th>Edit User</th>
                </tr>
            </thead>
            <tbody>
                @foreach (User user in users)
                {
                    <tr>
                        <td>
                            @user.Username
                        </td>
                        <td>
                            @user.Type
                        </td>
                        <td>
                            @user.Name
                        </td>
                        <td>
                            @user.Surname
                        </td>
                        <td>
                            @user.Email
                        </td>
                        <td>
                            @user.PhoneNumber
                        </td>
                        <td>
                            @{
                                if (user.Type == 'u')
                                {
                                    <a asp-controller="Admin" asp-action="ChangeUserType" asp-route-idUser="@user.Id" class="link-underline-hover">Make broker</a>
                                }
                                else if (user.Type == 'b')
                                {
                                    <a asp-controller="Admin" asp-action="ChangeUserType" asp-route-idUser="@user.Id" class="link-underline-hover">Remove broker</a>
                                }
                            }
                        </td>
                        <td>
                            <a asp-controller="Login" asp-action="SignUp" asp-route-idUser="@user.Id" asp-route-adminEdit="true" class="link-underline-hover">Edit Profile</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>   